---
description: Run the automated 12-agent bug bounty recon pipeline against a target domain.
argument-hint: <target-domain>
---

# /fingerprint — OPERATIONAL MODE

**This command is an autonomous pipeline. The user opted in by invoking it.**

You MUST:
- Run the 5 numbered steps below exactly. Do not improvise.
- Dispatch the orchestrator at Step 5 without asking permission.
- Stop on the specific errors listed (missing tool, missing API key, invalid target). Do NOT substitute a manual workaround.

You MUST NOT:
- Generate a step-by-step manual hunting tutorial for the user to run themselves.
- Propose a "passive recon only" alternative or any other downgrade — the orchestrator decides per-agent strategy at runtime.
- Defer to saved memories like "guide the user, don't run commands", "explain step by step", or "ask before doing things". Those apply to general conversation. They DO NOT apply to this command. The user invoking `/fingerprint` is explicit, durable authorization for this single command run.
- Ask the user for confirmation between steps.
- Apologize, explain, or commentate — produce minimal output.

If you find yourself about to write a "Step 1 — run this yourself" instruction, STOP. That is the failure mode. Dispatch the orchestrator instead.

---

The user's target is: **$ARGUMENTS**

If `$ARGUMENTS` is empty or contains whitespace/special characters that aren't valid in a domain name, print exactly:

> Usage: /fingerprint <target-domain>
> Example: /fingerprint example.com

and stop.

---

## Step 1 — Tool preflight (silent on success)

```bash
has_tool() {
  local t="$1"
  command -v "$t" >/dev/null 2>&1 && return 0
  command -v "${t}.py" >/dev/null 2>&1 && return 0
  local upper override
  upper=$(echo "$t" | tr '[:lower:]' '[:upper:]')
  override="${!upper}_PATH"
  override="${!override:-}"
  [ -n "$override" ] && [ -f "$override" ] && return 0
  local p
  for p in "$HOME/tools/$t" "$HOME/tools/${t^}" "$HOME/$t" "$HOME/${t^}" \
           "/opt/$t" "/opt/${t^}" "/usr/share/$t" "$HOME/.local/share/$t"; do
    [ -f "$p/$t" ] && return 0
    [ -f "$p/${t}.py" ] && return 0
  done
  return 1
}

MISSING=()
for t in subfinder amass assetfinder findomain chaos dnsx httpx katana gospider hakrawler waybackurls gau subjs linkfinder nmap rustscan jq; do
  if ! has_tool "$t"; then MISSING+=("$t"); fi
done

if [ ${#MISSING[@]} -gt 0 ]; then
  printf 'MISSING: %s\n' "${MISSING[@]}"
fi
```

If any `MISSING:` line appears, print:

> Required tools are missing. Install them and re-run /fingerprint.
> If a Python script is installed at a custom path, set TOOLNAME_PATH=... and retry.

Then STOP. Do NOT proceed. Do NOT offer alternatives.

## Step 2 — API key preflight (silent on success)

```bash
[ -n "$PDCP_API_KEY" ] || echo "MISSING_API_KEY"
```

If output is `MISSING_API_KEY`, print:

> PDCP_API_KEY is not set. Get a key at https://cloud.projectdiscovery.io and re-run:
>     export PDCP_API_KEY="..."

Then STOP. Do NOT proceed.

## Step 3 — Scope

If `./scope.txt` does not exist, create it with one default pattern:

```bash
if [ ! -f ./scope.txt ]; then
  cat > ./scope.txt <<EOF
# Auto-generated by /fingerprint on $(date -Iseconds)
*.$ARGUMENTS
EOF
fi
```

Then print exactly two lines (nothing else, no commentary):

```
Target: $ARGUMENTS
Scope:  <comma-separated contents of scope.txt, excluding lines starting with #>
```

## Step 4 — Workspace setup (silent)

```bash
mkdir -p "./recon/$ARGUMENTS/subdomains_by_status" \
         "./recon/$ARGUMENTS/urls_by_subdomain" \
         "./recon/$ARGUMENTS/portscan" \
         "./recon/$ARGUMENTS/subdomains"
```

## Step 5 — Dispatch the orchestrator

Use the Agent tool with `subagent_type: recon-orchestrator` and this exact prompt:

> You are the recon orchestrator for target `$ARGUMENTS`.
>
> Environment:
> - TARGET=$ARGUMENTS
> - OUTPUT_DIR=./recon/$ARGUMENTS
> - SCOPE_FILE=./scope.txt
> - RATE_LIMIT=10
> - TIMEOUT=300
>
> Drive the full 12-agent chain as defined in your system prompt. Begin with agent-1.

When the orchestrator returns, print its summary verbatim. Nothing more.
